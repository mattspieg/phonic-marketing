{
  "name": "phonic-marketing",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "cloudflare:whoami": "wrangler whoami",
    "dev": "vite --host 127.0.0.1",
    "typecheck": "tsc --noEmit",
    "check:worker": "node --check src/phonic-api.js",
    "check:attio": "node --check src/attio.js",
    "test:attio-name": "node --test tests/attio-name.test.mjs",
    "verify": "npm run typecheck && npm run check:worker && npm run check:attio && npm run test:attio-name",
    "deploy:worker": "npm run verify && wrangler deploy",
    "deploy:attio": "npm run verify && wrangler deploy --config wrangler.attio.toml",
    "deploy:assets": "wrangler pages deploy static-assets/public --project-name=phonic-static-assets"
  },
  "dependencies": {
    "@webflow/data-types": "2.0.0",
    "@webflow/react": "2.0.0",
    "react": "19.2.7",
    "react-dom": "19.2.7"
  },
  "devDependencies": {
    "@types/react": "19.2.17",
    "@types/react-dom": "19.2.3",
    "typescript": "6.0.3",
    "vite": "^8.0.16",
    "wrangler": "4.121.0"
  }
}
